思路：使用maxstep记录位置上最长升序字串的个数，遍历位置i处之前所有字符，获取最大升序字串长度。
# 最长上升子序列
while True:
    try:
        n = int(input())
        line = map(int, input().split())
        maxstep = [1]
        for i in range(1, len(line)):
            res = 1
            for j in range(0, i):
                if line[j] < line[i]:
                    maxstep = max(maxstep[j] + 1, res)
        print(max(maxstep))
    except:
        break
